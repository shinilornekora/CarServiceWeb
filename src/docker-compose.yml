version: '3.8'
services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: shiniasse
      POSTGRES_PASSWORD: shinlass3
      POSTGRES_DB: cars
    ports:
      - "5432:5432"